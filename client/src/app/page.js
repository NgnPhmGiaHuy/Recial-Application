import Image from 'next/image'

import {Header} from "@/components";

const Home = () => {
    return (
        <div>
            <Header navigationItem="home"/>
        </div>
    );
};

export default Home;